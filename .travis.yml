sudo: false

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.m2

language: android

#jdk:
#  - oraclejdk8

android:
  components:
    - tools
    - platform-tools
    # to find the available buidl tools version
    # cd $ANDROID_HOME
    # e.g. in /d/android/SDK/tools/bin
    # $ ./sdkmanager.bat --list | grep build
    - build-tools-27.0.3
    - android-27
#licenses:
#  - 'android-.+'
#  - 'android-.+'
#  - 'intel-android.+'
#
## another workaround for newer licensing issue
## - https://stackoverflow.com/a/42736695/6287240
## - https://stackoverflow.com/a/47726910/6287240
## - https://github.com/travis-ci/travis-ci/issues/6555#issuecomment-282635361
#before_install:
# - yes | sdkmanager "platforms;android-27"
# - yes | sdkmanager "build-tools-27.0.3"
# - chmod +x gradlew

script:
  - ./gradlew clean check assemble
